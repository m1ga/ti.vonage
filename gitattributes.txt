# TODO: Rename this file to ".gitattributes" once merged and push the framework.
#       This is currently not possible because forks cannot add new git-lfs files 
ios/platform/** filter=lfs diff=lfs merge=lfs -text
